<!DOCTYPE html>
<html>
  <head>
    <title>Billboard</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <div class="ui container">
    <%= render 'layouts/navbar' %>
    <%= yield %>
    </div>
  </body>
</html>
